namespace Phonestore.Domain.Enum
{
    public enum StatusCode
    {
        OrderNotFound = 10,
        PhoneNotFound = 20,
        UserNotFound = 30,
        UserAlreadyExists = 40,
        OK = 50,
        EntityNotFound = 60,
        InternalServerError = 500
    }
}
